# AI Memory — Control Center

> Thin digest. Bootstraps a session in <30 seconds. **Canonical truth lives elsewhere — see the link table.** Update the *Hot Context* section after each task; canonical docs (`docs/PROGRESS.md`, `docs/DECISIONS.md`) get the long-form on session close.

---

## Project Pulse

**Slice-2 fully shipped. Slice-3 polish in flight.** Last shipped (2026-05-06): KV-backed lead-form rate limit, /countries combobox, /search country-dropdown + green map pins + centroid fallback, agent-profile enrichment (bio, languages, specialties, areas-served, FAQs + FAQPage/BreadcrumbList JSON-LD, dashboard FAQ editor at `/dashboard/faqs`), pricing OG image.

---

## Canonical Links — protocol role → file

| Role (per protocol)        | Canonical file                          | Notes                                                                    |
|----------------------------|-----------------------------------------|--------------------------------------------------------------------------|
| **Decision log**           | `docs/DECISIONS.md`                     | Architectural choices + WHY. Append-only.                                |
| **Velocity / per-session** | `docs/PROGRESS.md`                      | Newest entries on top. Mandatory append at session close (rule #7).      |
| **Risks / warnings**       | `docs/RISKS.md`                         | Live register; R-numbered.                                               |
| **Open questions**         | `docs/OPEN_QUESTIONS.md`                | Items blocked on PO; pruned as decisions land.                           |
| **Spec / source of truth** | `docs/HANDOFF.md` + `HANDOFF_part2.md`  | If implementation deviates, same PR updates the spec (rule #6).          |
| **Project memory**         | `CLAUDE.md`                             | High-signal, ~200 lines. Rules, conventions, env quirks.                 |
| **Critique / slice plan**  | `docs/CRITIQUE.md` §F                   | Slice 2/3 plan — read before re-scoping anything.                        |
| **Cloudflare resources**   | `docs/CLOUDFLARE_RESOURCES.md`          | What's provisioned vs. pending.                                          |
| **DNS records**            | `docs/DNS.md`                           | Brevo apex records; Pages domain pending.                                |

This file is **not authoritative** — if anything here disagrees with the canonical doc, the canonical doc wins and this file gets corrected.

---

## Hot Context (rolling — last ~24h, not yet promoted)

- **Auth Turnstile diagnostic improved** — error-callback now surfaces CF error codes. Root cause of PO's "Bot challenge failed" was Phantom wallet's `inpage.js` injecting into the challenge iframe. Client-side, no code fix needed beyond the diagnostic.
- **Workers Paid plan in effect** — upgraded after hitting 3 MiB worker size limit. $5/mo. Documented in `docs/DECISIONS.md`.
- **PR 2 dashboard FAQ editor** uses Supabase browser client subject to RLS (no new API surface). Migration 0032 enforces owner/manager-only writes.
- **Pre-existing test failures** that aren't from current work:
  - `tests/unit/billing-founder-window.test.ts` — date-drift bug (asserts window-closed at 2024-01-01; today is 2026-05-06).
  - All 9 RLS tests need `.env.local` sourced; they fail in clean shell.
- **Country centroids** seeded in `src/lib/listings/country-centroids.ts` for ~70 markets. Add codes as new countries onboard agents.
- **`saved_searches` already has `last_seen_at`** but that tracks user-side dashboard opens, not server-side notify dispatches — semantics will diverge. Saved-search worker needs additive migration 0033 with new `last_notified_at` column. Don't reuse `last_seen_at`.

---

## Slice-3 Finalization — what's left

Sequenced by value × autonomy. Items marked **PO** are blocked on a product-owner decision and I will not touch without Your sign-off.

1. **Saved-search email-alert worker** — autonomous, ~3h. Saved-searches surface is live but inert beyond storage; workers diff each query against `last_notified_at` and dispatches a Brevo digest. Architecture: CF Cron Trigger + dedicated Worker `aho-saved-search-worker` (Pages Functions don't support cron natively). May need migration 0033 (`saved_searches.last_notified_at` if column doesn't exist).
2. **Webhook-replay fixture-state harness** — autonomous, ~2h. Pre-live Stripe confidence on the 5 deferred replay cases.
3. ~~**OG image coverage gaps**~~ — done. Inventory revealed only `pricing` was a real gap (search is `noindex`, rest auth-gated/inherits). Pricing OG shipped.
4. **(PO)** Featured-listings ranking — how does ranking compute? Decision needed.
5. **(PO)** Plus tier — ship, soften, or remove from marketing copy?
6. **(PO)** Neighborhood overlays — needs polygon data source decision.
7. List-view sync to bbox — UX nice-to-have, low priority.

---

## Active Warnings (from `RISKS.md` + lessons)

- **Real-only data rule.** No demo listings, no stock photos in user-facing slots. Test fixtures stay in `tests/`. Empty states are honest emptiness.
- **No `pnpm dev`.** Dev/preview/prod all on Cloudflare Pages via `git push`. Don't start `next dev`.
- **Env loading.** `.env.local` is not auto-loaded. Prefix bash commands: `set -a && source .env.local && set +a && <cmd>`.
- **pnpm via corepack.** Always `corepack pnpm@9.12.3 …` everywhere.
- **Stripe = TEST mode.** Live products archived 2026-04-29. Promotion to live requires a `DECISIONS.md` entry (rule #9).
- **RLS test fixtures share prod Supabase.** Public surfaces filter `aho-test-org-%` slugs + `aho-fixture-%` listing slugs (R11).
- **Frontend never decides authz.** Always ask the database. RLS is primary tier enforcement (rule #4 + #2).

---

## Maintenance contract

- This file gets touched **after every task that lands**. Promote stable items into `docs/PROGRESS.md` (per-session) and `docs/DECISIONS.md` (architectural) at session close.
- If this file disagrees with canonical, canonical wins; fix this file.
- Keep under ~120 lines. Past that, prune Hot Context — old items are already in PROGRESS.md.
