{
  "name": "aho-instagram-drift",
  "version": "0.1.0",
  "private": true,
  "description": "AHO scheduled worker — daily proxy to /api/cron/instagram-drift on the Pages app. Detects agents who newly linked an IG Business account in Meta Business Suite + emails them to click Reconnect. See ../../docs/INSTAGRAM_SHARING_PLAN.md Phase 3.",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "trigger": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.88.0"
  }
}
