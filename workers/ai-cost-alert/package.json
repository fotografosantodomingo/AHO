{
  "name": "aho-ai-cost-alert",
  "version": "0.1.0",
  "private": true,
  "description": "AHO scheduled worker — daily proxy to /api/cron/ai-cost-alert. Emails the operator when yesterday's AI spend or per-audit cost crosses configured thresholds. See ../../docs/SUPER_PRO_STAGE_1_PLAN.md §7.",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "trigger": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.88.0"
  }
}
